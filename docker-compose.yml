services:
  calendar-sync:
    build: .
    container_name: calendar-sync
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - calendar_sync_data:/data

volumes:
  calendar_sync_data:
